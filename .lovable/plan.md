## Align onboarding experience level boxes with profile page\n\nIn `src/components/Onboarding.tsx` (step 2, level picker), update the level button grid to match the profile page version so headers and hint text align across all four boxes.\n\n### Changes in `src/components/Onboarding.tsx`\n\n1. Add `auto-rows-fr` to the grid wrapper.\n2. Add `flex flex-col` to each level button so children stack consistently.\n3. Add `min-h-[1rem]` to the level title div.\n4. Add `min-h-[2.25rem]` to the hint div.\n5. Switch button padding/rounded to match profile (`p-3 rounded-xl`) — currently `p-3 rounded-2xl`; profile uses `rounded-xl`. Use `rounded-xl` for parity.\n6. Title font size: profile uses